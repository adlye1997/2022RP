/**
  ******************************************************************************
  * @file           : config_vision.c\h
  * @brief          : 
  * @note           : 2022-1-18 13:53:19
  ******************************************************************************
  */
	
#include "config_vision.h"
#include "string.h"

/**
  * @brief  视觉数据接收到rx_info后再此更新
  * @param  
  * @retval 
  */
void vision_update(vision_t *vision)
{
//	memcpy(&vision->info->example, &vision->rx_info->datau8_1, 1);
//	memcpy(&vision->info->example, (void*)&vision->rx_info->dataf_1, 4);
//	memcpy(&vision->info->example, (void*)&vision->rx_info->dataf_2, 4);
//	memcpy(&vision->info->example, (void*)&vision->rx_info->dataf_3, 4);
//	memcpy(&vision->info->example, &vision->rx_info->->datau8_2, 1);
//	memcpy(&vision->info->example, &vision->rx_info->datau8_3, 1);
//	memcpy(&vision->info->example, &vision->rx_info->datau8_4, 1);
//	memcpy(&vision->info->example, &vision->rx_info->datau8_5, 1);
//	memcpy(&vision->info->example, &vision->rx_info->datau8_6, 1);
//	memcpy(&vision->info->example, &vision->rx_info->datau8_7, 1);
}
